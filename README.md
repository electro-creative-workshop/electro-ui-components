# Electro UI Components

## Installation

```
npm install --save github:electro-creative-workshop/electro-ui-components
```

## Development

To run the demo execute `npm run dev` from the root of the repository after `npm install`.